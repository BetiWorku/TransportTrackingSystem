<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ REQUIRED PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TransportTrackingSystem">

        <!-- 🗺️ GOOGLE MAPS API KEY -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDuWcck4kzdIBozVhhE6Ftqt3GoBgOfMvc"/>

        <!-- 🚀 SPLASH SCREEN (New Launcher) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TransportTrackingSystem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ForgotPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ResetPasswordActivity"
            android:exported="true">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="transportation-tracker-app.firebaseapp.com"
                    android:pathPrefix="/__/auth/action" />
            </intent-filter>
        </activity>

        <!-- 🚀 WELCOME PAGE -->
        <activity android:name=".activities.WelcomeActivity" />

        <!-- 🚀 REGISTER ACTIVITY -->
        <activity android:name=".activities.RegisterActivity" />

        <!-- 🚀 OTP VERIFICATION ACTIVITY -->
        <activity android:name=".activities.OtpVerificationActivity" />

        <!-- 🚀 MAIN MAP ACTIVITY -->
        <activity 
            android:name=".activities.MainActivity"
            android:exported="false" />

        <!-- 🚀 SETTINGS ACTIVITY -->
        <activity android:name=".activities.SettingsActivity" />

        <!-- 🚀 BUS DETAILS ACTIVITY -->
        <activity android:name=".activities.BusDetailsActivity" />

        <!-- 🚀 NEWS ACTIVITY -->
        <activity android:name=".activities.UserNewsActivity" />

        <!-- 🚀 BUS TRACKER ACTIVITY -->
        <activity android:name=".activities.BusTrackerActivity" />

        <!-- 🚀 COMPLAINT ACTIVITY -->
        <activity 
            android:name=".activities.ComplaintActivity"
            android:parentActivityName=".activities.MainActivity" />

        <activity android:name=".activities.UserDashboardActivity" />

        <receiver android:name=".utils.NotificationActionReceiver" android:exported="false">
            <intent-filter>
                <action android:name="ACTION_MUTE" />
                <action android:name="ACTION_ALARM" />
            </intent-filter>
        </receiver>

    </application>

</manifest>